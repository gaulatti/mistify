# Local/deployment overrides only. Application defaults live in code.

HTTP_PORT=8000
GRPC_PORT=50000
VALKEY_HOST=192.168.0.99
VALKEY_PORT=6379

# Clustering thresholds (lower = more permissive / higher recall)
# CLUSTERING_SIM_ENTITY=0.35
# CLUSTERING_SIM_GLOBAL=0.50
# CLUSTERING_AVG_SIM_MIN=0.40
# CLUSTERING_PRE_FILTER_MIN_SIM=0.25
# CLUSTERING_MAX_CANDIDATES=30
